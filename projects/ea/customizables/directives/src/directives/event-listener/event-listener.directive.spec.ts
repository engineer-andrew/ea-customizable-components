import { EaEventListenerDirective } from './event-listener.directive';

describe('EaEventListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new EaEventListenerDirective();
    expect(directive).toBeTruthy();
  });
});
